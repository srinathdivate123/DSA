// Given a row-wise sorted matrix of size r*c, where r is no. of rows and c is no. of columns, find the median in the given matrix.
// Assume r*c is odd
#include <bits/stdc++.h>
using namespace std;


// Brute is to fill all elements in a array sort using the sort function, and then find the middle value

//

int main()
{
    
    return 0;
}